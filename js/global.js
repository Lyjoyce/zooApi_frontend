// global.js
// Ce fichier contient les petites fonctions utilisables sur plusieurs pages
// (ex. manipulation générique, vérification de connexion, etc.)

document.addEventListener("DOMContentLoaded", () => {
  // Affiche dans la console un message à chaque chargement de page (debug simple)
  console.log("Page chargée : script global.js actif");
});
